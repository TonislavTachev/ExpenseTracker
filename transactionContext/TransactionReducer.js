import {GET_PROFIT, GET_EXPENSE, DELETE_PROFIT, DELETE_EXPENSE} from '../types'


export default (state,action) =>{
  
     switch (action.type) {
         case GET_PROFIT:
             
             break;
     
         default:
             break;
     }

}